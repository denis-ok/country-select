module Style = {
  module Size = {
    let menuWidthPx = 230;

    let menuHeightPx = 200;

    let menuOptionHeightPx = 26;

    let searchHeightPx = 35;
  };

  module Color = {
    let dropdownBorder = `rgba((0, 0, 0, 0.2));

    let menuBorder = `rgba((0, 0, 0, 0.08));

    let activeItemBackground = `rgb((235, 235, 235));

    let hover = `rgb((245, 245, 245));

    let focus = `hex("3b78e7");
  };

  let fontSizePx = `px(14);

  let fontFamily = `custom("Arial");
};
